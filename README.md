Utility library for comparing 2 case classes or collections without selected fields to exclude

create jar:
`sbt publishLocal`

add it to your Dependencies:
`libraryDependencies += "com.niro" % "equals-ignoring-fields"% "0.1"`